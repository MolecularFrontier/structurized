package tech.molecules.structurized.mmp;

/** Outcome category for connector-aware assembly of an MMP key and variable fragment. */
public enum MmpFragmentAssemblyStatus {
    ASSEMBLED,
    INVALID_KEY,
    INVALID_VALUE,
    INVALID_PRODUCT
}
